export enum Language {
  AddressIncrement = '>',
  AddressDecrement = '<',
  ValueIncrement = '+',
  ValueDecrement = '-',
  PrintValue = '.',
  InputValue = ',',
  JumpForward = '[',
  JumpBackward = ']',
}

export enum AdditionLanguage {
  Comment = ';',
  PrintDebug = '#',
}
